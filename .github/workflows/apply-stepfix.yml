name: apply-stepfix

on:
  push:
    paths:
      - '.buildtmp/stepfix/GO.marker'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Assemble and apply combined patch
        run: |
          set -e
          cat .buildtmp/stepfix/p*.b64 | tr -d '\n' | base64 -d > /tmp/stepfix.patch
          wc -c /tmp/stepfix.patch
          git apply --whitespace=nowarn /tmp/stepfix.patch
          rm -rf .buildtmp/stepfix
          rmdir .buildtmp 2>/dev/null || true
          rm -f .github/workflows/apply-stepfix.yml .github/workflows/apply-fix3.yml

      - name: Commit and push
        run: |
          set -e
          git config user.name "apply-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "fix: X follower lookup, PageSpeed retry, banner upload + crop preview (automated)

          Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
          git push origin HEAD:main
