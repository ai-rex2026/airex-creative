name: tmp-fix-social-insights
on:
  push:
    paths:
      - ".buildtmp/social-insights-fix/GO.marker"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: |
          git config user.name "airex-bot"
          git config user.email "bot@ad-rex.com"
          git apply --whitespace=fix .buildtmp/social-insights-fix/patch.diff
      - name: Clean up temp files and this workflow
        run: |
          rm -rf .buildtmp/social-insights-fix
          rm -f .github/workflows/tmp-fix-social-insights.yml
      - name: Commit and push
        run: |
          git add -A
          git commit -m "fix: social-insights.ts の転記ミス2箇所を修正 (temp workflow self-clean)"
          git push origin HEAD:main
